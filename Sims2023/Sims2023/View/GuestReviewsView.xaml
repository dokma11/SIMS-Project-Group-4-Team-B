<Window x:Class="Sims2023.View.GuestReviewsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sims2023.View"
        mc:Ignorable="d"
        Title="GuestReviewsView" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <DataGrid>
            <DataGrid.Columns ItemsSource="{Binding ReviewsToDisplay}" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGridTextColumn Header="Ime" Binding="{Binding Review.User.Name}"/>
                <DataGridTextColumn Header="Prezime" Binding="{Binding Review.User.Surname}"/>
                <DataGridTextColumn Header="Korisnicko ime" Binding="{Binding Review.User.UserName}"/>
                <DataGridTextColumn Header="Kljucna tacka na kojoj se prikljucio" Binding="{Binding Review.KeyPoint}"/>
                <DataGridTextColumn Header="Ocena" Binding="{Binding Review.Grade}"/>
                <DataGridTextColumn Header="Validna recenzija" Binding="{Binding Review.IsValid}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
